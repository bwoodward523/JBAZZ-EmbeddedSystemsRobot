# AICameraInterface - Raspberry Pi AI camera (on-camera inference only)
# Install on the Pi first: sudo apt install -y python3-picamera2 imx500-models
numpy>=1.19.0
opencv-python-headless>=4.5.0
picamera2>=1.1.0
imx500-models>=1.0.0
#sudo apt install -y python3-picamera2 imx500-models

